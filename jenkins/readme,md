create namespace > "kubectl create ns jenkins"
create pv and pvc > "kubectl apply -f pv.yaml pvc.yaml"

1. helm repo add https://charts.jenkins.io
2. helm install jenkins jenkins/jenkins --values "jenkins.yaml" -n "jenkins"